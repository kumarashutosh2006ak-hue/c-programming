#include <stdio.h>
#include <math.h>
int main()
{
    double pi;
    pi=M_PI;
    
    float area;
    float r;
    
    printf("enter the radius of the circle: ");
    scanf("%f",&r);
    
    area=pi*r*r;

    // CHANGED: %.2f tells C to round and stop at 2 decimal places
    printf("the area of the circle is %.3f sq units",area);
    
    return 0;
}
