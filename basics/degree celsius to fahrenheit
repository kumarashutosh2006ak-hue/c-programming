#include <stdio.h>

int main()
{
    float Celsius;
    printf("enter the tempreature in degree Celsius: ");
    scanf("%f",&Celsius);
    
    float Fahrenheit = (Celsius*1.8)+32;
    printf("%.2f",Fahrenheit);

    return 0;
}
