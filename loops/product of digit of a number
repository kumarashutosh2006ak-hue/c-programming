#include <stdio.h>

int main()
{
    int n;
    int prd=1;
    int temp;
    
    printf("enter the number you want the sum of: ");
    scanf("%d",&n);
    
    while(n>0){
        temp=n%10;
        prd=prd*temp;
        n=n/10;
    }
    printf("%d",prd);

    return 0;
}
