#include <stdio.h>

int main()
{
    int n;
    int sum=0;
    int temp;
    
    printf("enter the number you want the sum of: ");
    scanf("%d",&n);
    
    while(n>0){
        temp=n%10;
        sum=sum+temp;
        n=n/10;
    }
    printf("%d",sum);

    return 0;
}
