#include <stdio.h>

int main()
{   
    //initialisation
    int a,b,c;
    
    printf("enter 1st number: ");
    scanf("%d",&a);
    
    printf("enter 2nd number: ");
    scanf("%d",&b);
    
    printf("enter 3rd number: ");
    scanf("%d",&c);
    
    //logic
    
    if(a>=b && a>=c){
        printf("number %d the greatest\n",a);
    }
    else if(b>=a && b>=c){
        printf("number %d is the greatest\n",b);
    }
    else if(c>=a && c>=b){
        printf("number %d is the greatest\n",c);
    }
    
    

    return 0;
}
